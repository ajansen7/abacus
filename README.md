# abacus
a locally hosted, remotely accessible orchestration framework designed to manage headless agentic workflows. It serves as an agnostic engine that handles API routing, webhook listening, and cross-session memory management using a single graph-database solution (Beads).
